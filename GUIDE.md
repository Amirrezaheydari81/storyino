# Storyino Guide

## معرفی

Storyino یک پلاگین وردپرس برای نمایش استوری تصویر/ویدیو به صورت مینیمال است.

این پلاگین با شورت‌کد کار می‌کند و می‌تواند استوری‌ها را از کتابخانه رسانه وردپرس انتخاب کند.

---

## نصب سریع

1. پوشه `storyino` را داخل مسیر زیر قرار بده:

```txt
wp-content/plugins/storyino
```

2. پلاگین را از پیشخوان وردپرس فعال کن.

3. از منوی **Storyino** داخل پیشخوان، تصویر یا ویدیوها را انتخاب کن.

4. دکمه **ذخیره تغییرات** را بزن.

5. داخل هر صفحه/نوشته که خواستی از شورت‌کد استفاده کن:

```txt
[storyino]
```

---

## انتخاب استوری از رسانه وردپرس

در پیشخوان:

```txt
Storyino
```

روی دکمه زیر بزن:

```txt
+ انتخاب از کتابخانه رسانه
```

سپس:

- می‌تونی چند تصویر انتخاب کنی.
- می‌تونی چند ویدیو انتخاب کنی.
- محدودیت تعداد وجود ندارد.
- می‌تونی با کشیدن آیتم‌ها، ترتیب نمایش را عوض کنی.
- با دکمه × هر آیتم را حذف کنی.

در پایان:

```txt
ذخیره تغییرات
```

را بزن.

---

## شورت‌کد اصلی

```txt
[storyino]
```

این شورت‌کد یک دکمه می‌سازد. با کلیک روی دکمه، استوری‌ها باز می‌شوند.

---

## پارامترهای شورت‌کد

| پارامتر    | توضیح                                | مقدار پیش‌فرض |
| ---------- | ------------------------------------ | ------------- |
| `label`    | متن دکمه                             | مشاهده استوری |
| `ids`      | آی‌دی فایل‌های رسانه، جداشده با کاما | خالی          |
| `speed`    | شبیه‌سازی سرعت دانلود بر حسب KB/s    | 250           |
| `duration` | مدت نمایش هر تصویر بر حسب میلی‌ثانیه | 5000          |

---

## مثال‌ها

### نمایش دکمه با متن دلخواه

```txt
[storyino label="دیدن استوری محصول"]
```

---

### نمایش بدون شبیه‌سازی سرعت دانلود

```txt
[storyino speed="0"]
```

---

### تغییر زمان نمایش عکس‌ها

```txt
[storyino duration="7000"]
```

---

### استفاده از آی‌دی‌های خاص Media Library

```txt
[storyino ids="12,18,25"]
```

این حالت اولویت دارد و لیست انتخاب‌شده در تنظیمات را نادیده می‌گیرد.

---

## اولویت منابع استوری

وقتی شورت‌کد اجرا می‌شود، Storyino به این ترتیب استوری‌ها را پیدا می‌کند:

1. اگر داخل شورت‌کد `ids` نوشته شده باشد، همان‌ها استفاده می‌شوند.
2. اگر لیستی در صفحه تنظیمات Storyino انتخاب شده باشد، همان استفاده می‌شود.
3. اگر فیلتر یا استوری‌های پیش‌فرض وجود داشته باشد، آن‌ها استفاده می‌شوند.
4. در نهایت فایل‌های داخل `assets/media` می‌توانند fallback باشند.

---

## فایل‌های مهم

```txt
storyino/storyino.php
storyino/includes/shortcode.php
storyino/includes/admin.php
storyino/includes/helpers.php
storyino/assets/css/storyino.css
storyino/assets/css/storyino-admin.css
storyino/assets/js/storyino.js
storyino/assets/js/storyino-admin.js
```

---

## نکات فنی

### پخش ویدیو

برای اینکه ویدیو بدون تعامل کاربر پخش شود:

- ویدیو به صورت `muted` پخش می‌شود.
- بهتر است ویدیوها `MP4/H.264` باشند.
- بهتر است حجم ویدیوها بهینه باشد.

---

### دانلود کامل قبل از پخش

در حالت فعلی، پلاگین می‌تواند فایل را به صورت Blob دانلود کند و بعد از آماده شدن، تایمر را شروع کند.

اگر نمی‌خواهی شبیه‌سازی سرعت فعال باشد:

```txt
[storyino speed="0"]
```

---

### بهینه‌سازی

- برای پروداکشن `speed="0"` بگذار.
- ویدیوهای سنگین را compress کن.
- تصاویر را با فرمت WebP یا JPG بهینه آپلود کن.
- تعداد استوری‌ها را خیلی زیاد نکن، مگر اینکه فایل‌ها سبک باشند.

---

## عیب‌یابی

### دکمه ظاهر نمی‌شود

- مطمئن شو شورت‌کد داخل محتوا باشد.
- مطمئن شو پلاگین فعال است.
- مطمئن شو قالب از `wp_footer()` استفاده می‌کند.

---

### استوری باز می‌شود ولی فایل خطا می‌دهد

- فایل رسانه واقعا آپلود شده باشد.
- لینک فایل در دسترس باشد.
- اگر ویدیو است، فرمت آن MP4 باشد.
- اگر سایت HTTPS است، فایل‌ها با لینک HTTP لود نشوند.

---

### ویدیو پخش نمی‌شود

- ویدیو باید برای پخش خودکار mute باشد.
- مرورگرها اجازه autoplay با صدا را نمی‌دهند.
- فرمت پیشنهادی:

```txt
MP4 / H.264 / AAC
```

---

### تایمر حرکت نمی‌کند

این حالت معمولاً وقتی فایل هنوز آماده نشده است.

- کنسول مرورگر را چک کن.
- مطمئن شو فایل رسانه خطای 404 ندارد.
- اگر شبیه‌سازی سرعت فعال است، `speed="0"` تست کن.
